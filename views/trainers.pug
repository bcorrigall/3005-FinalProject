doctype html
head
    meta(charset='UTF-8')
    title Home Page
    link(href='/css/style.css' rel='stylesheet')
body
    include ./partials/header
    h4 
    .list
        for trainer in result
            a(href=`/trainers/${trainer.t_id}`) #{trainer.name}
            br